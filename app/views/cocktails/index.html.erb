<h1>Cocktails</h1>

<% @cocktails.each do |cocktail| %>
<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title"><%= cocktail.name %></h5>
    <h6 class="card-subtitle mb-2 text-muted"><%= cocktail.name %></h6>
    <p class="card-text">Description</p>
    <a href="#" class="card-link">Link to cocktail</a>
    <% end %>
  </div>
</div>

